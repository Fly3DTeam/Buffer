# 更新日志

本文件集中记录 Fly Buffer 缓冲器固件的版本变化。README 仅保留当前固件的功能、操作方法、注意事项和默认参数。

## 2.0.3 - 2026-08-15

### 新增

- 新增 TPU 与非 TPU 两种耗材模式，同时按下两个按键切换。
- 耗材模式保存到 EEPROM，掉电重启后保持；旧版本升级后默认进入非 TPU 模式。
- USB CDC 启动信息和 `info` 命令增加当前耗材模式、快速模式、保持电流档位及失能时间输出。
- README 增加可配置默认参数和主要固定参数表。

### 调整

- TPU 模式关闭快速补偿，停止后使用 `IHOLD=0` 的 `1/32` 满量程保持电流，静止 `30s` 后失能。
- TPU 模式运行电流固定为 `400mA RMS`；非 TPU 模式继续使用可配置电流，默认 `500mA RMS`。
- 显式设置并监控 `PWMCONF.freewheel=0`，确保 `IHOLD=0` 保持正常电流调节，不进入自由滑行或被动制动。
- 非 TPU 模式启用快速补偿，电机停止后立即失能。
- TPU 模式有料时，耗材灯与状态灯反相交替闪烁；非 TPU 模式有料时耗材灯常亮。
- 取消 TPU/非 TPU 模式切换时的五次闪烁和长亮熄灭提示，状态灯持续显示当前工作或故障状态。
- 单键长按手动运动改为最高优先级，按住期间忽略三路缓冲光感，包括 HALL2 停止位；松开后恢复传感器保护。
- 快速补偿恢复普通速度时按 `accel` 平滑减速，减速完成后再恢复普通电流和静音斩波模式。
- TPU 模式确认无耗材后不再强制停机，仍按三路缓冲位置传感器正常工作；断料输出和耗材灯提示保持不变。

### 修复

- 减少高速状态恢复普通速度时，由电流和斩波模式提前切换造成的明显噪声。
- 停止运动时保证高速电流正确恢复为普通运行电流。

## 2.0.2 - 2026-08-11

- 固件版本更新为 `2.0.2`。
- 电机停止后不再立即失能，保留静止保持电流，并在连续静止 `30s` 后失能。
- 下一次自动或手动运动开始前自动重新使能驱动器。
- 完善暂停、前进超时和状态灯的区分及恢复逻辑。

## 2.0.1 - 2026-06-16

- 固件版本更新为 `2.0.1`。
- 电机控制改为 STEP/DIR 定时器脉冲输出。
- 速度单位改为 `mm/s`，新增 `accel` 加速度设置命令。
- 使用 BMG 齿轮参数进行步进脉冲换算。
- 普通与高速运动统一使用 16 细分，降低 `100mm/s` 时的 STEP 中断压力。
- 运动超过 `100mm` 且缓冲位置未变化时，自动提升到 `100mm/s`。
- 高速补偿时提高电流并切换高速斩波模式，退出后恢复普通电流和静音模式。
- TMC2225 改用 TMC2208 兼容寄存器类通信。
- 增加 TMC 状态和配置定期检查，配置不匹配时自动重发。
- 兼容 TMC2225 `GCONF.en_spreadCycle` 读写反相现象。
- 优化看门狗喂狗逻辑和高速脉冲输出稳定性。
- 停止位光感中断可立即关闭 STEP 脉冲，提高停止响应速度。
- 断料逻辑改为无料后继续工作 `10s`，仅延迟断料通知。
- 前进超时错误支持在重新上料后自动恢复。
- 修复 `steps` 为零、电流越界等参数安全问题。
